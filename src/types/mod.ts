export interface ModInfo {
  name: string;
  author: string;
  version: string;
  description: string;
  uniqueId: string;
  MinimumApiVersion: string;
  UpdateKeys: string[];
  last_update: string;
  enabled: boolean;
  updatable: boolean;
} 